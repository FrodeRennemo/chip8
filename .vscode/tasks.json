{
    "version": "2.0.0",
    "tasks": [
        {
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "taskName": "build",
            "command": "cargo",
            "type": "process",
            "args": [
                "build"
            ],
            "problemMatcher": [
                "$msCompile"
            ]
        }
    ]
}